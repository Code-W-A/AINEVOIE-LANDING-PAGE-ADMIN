rules_version = '2';

// TEMPORARY: fully open rules for local/testing. Restore strict rules before production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
